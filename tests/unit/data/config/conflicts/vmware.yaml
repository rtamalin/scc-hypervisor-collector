---

# VMWare Hypervisor Backends
backends:

  # A conflict with what's in backends.yaml
  - id: "multiple_VCenter_1"
    module: "VMware"
    hostname: "vcenter3.example.com"
    port: 443
    username: "VMware_Account_Username"
    password: "VMware_Account_Password"
